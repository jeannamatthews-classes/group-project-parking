<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#004D40"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/detailLotName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/white"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/detailLotUsage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="#B2DFDB"
        android:textSize="20sp" />

    <Button
        android:id="@+id/btnGoogleMaps"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:backgroundTint="#FFC107"
        android:text="@string/navigate_with_google_maps"
        android:textColor="@android:color/black" />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">


        <ImageButton
            android:id="@+id/back_button"
            android:layout_width="100dp"
            android:layout_height="70dp"
            android:layout_gravity="start"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:contentDescription="@string/back"
            android:scaleType="fitCenter"
            android:src="@drawable/back_icon"
            app:tint="@android:color/white" />


        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical">


            <ImageButton
                android:id="@+id/Favorites"
                android:layout_width="100dp"
                android:layout_height="80dp"
                android:layout_gravity="end"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:backgroundTint="#FFC107"
                android:contentDescription="@string/favorite_star"
                android:padding="8dp"
                android:scaleType="fitStart"
                android:src="@drawable/non_colored_star"
                android:textColor="@color/gold" />

            <ImageButton
                android:id="@+id/Remove_Favorites"
                android:layout_width="100dp"
                android:layout_height="80dp"
                android:layout_gravity="end"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:backgroundTint="#FFC107"
                android:contentDescription="@string/remove_favorite_star"
                android:padding="8dp"
                android:scaleType="fitStart"
                android:src="@drawable/full_colored_star"
                android:textColor="@android:color/black" />

        </FrameLayout>

    </LinearLayout>

</LinearLayout>